# Test-only dependencies. Kept out of requirements.txt so the production
# install stays the app and nothing else.
-r requirements.txt

pytest>=7.0

# Reads the hero photograph so `tests/test_hero_legibility.py` can size the
# backdrop's shade against the picture's real pixels instead of a worst case.
# Needs WebP support, which the wheels ship with.
pillow>=10.4
